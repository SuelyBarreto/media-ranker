<section class="spotlight">
    <h2 class="spotlight__header">
      <span class="spotlight__header--prefix">Media Spotlight</span>
      <%= link_to @spotlight.title, work_path(@spotlight.id), class:"spotlight__link-to" %> 
      by <%= @spotlight.creator %>
    </h2>
    <p class="spotlight__description">
      <%= @spotlight.votes %> votes - <%= @spotlight.description %>
    </p>

</section>

<hr class="root__hr" />

<section class="top-ten__container">
  <section class="top-ten__list-container">
  <h2 class="top-ten__header">
    Top Movies
  </h2>
  <ul class="list-group top-ten__list">
    <% @top_movies.each do |work|  %>
      <li class="list-group-item">
        <h4>
        <%= work.title %>
        <%= link_to work.title, work_path(work.id) %>
        <small class="top-ten__creator"> by <%= work.creator %></small>
        </h4>
        <p><%= work.votes %> votes</p>
      </li>
    <% end %>
  </ul>
</section>

  <section class="top-ten__list-container">
  <h2 class="top-ten__header">
    Top Books
  </h2>
  <ul class="list-group top-ten__list">
    <% @top_books.each do |work|  %>
      <li class="list-group-item">
        <h4>
        <%= work.title %>
        <%= link_to work.title, work_path(work.id) %>
        <small class="top-ten__creator"> by <%= work.creator %></small>
        </h4>
        <p><%= work.votes %> votes</p>
      </li>
    <% end %>
  </ul>
</section>

  <section class="top-ten__list-container">
  <h2 class="top-ten__header">
    Top Albums
  </h2>
  <ul class="list-group top-ten__list">
    <% @top_albums.each do |work|  %>
      <li class="list-group-item">
        <h4>
        <%= work.title %>
        <%= link_to work.title, work_path(work.id) %>
        <small class="top-ten__creator"> by <%= work.creator %></small>
        </h4>
        <p><%= work.votes %> votes</p>
      </li>
    <% end %>
  </ul>
</section>

</section>
